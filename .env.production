# Mailikan Production Environment Configuration
# Kopieren Sie diese Datei auf den Server und passen Sie die Werte an

# Server Configuration
NODE_ENV=production
PORT=3000

# Session Secret (ÄNDERN SIE DIES!)
SESSION_SECRET=change-this-to-a-random-string-in-production

# Security Settings
SECURE_COOKIES=true

# Application Settings
APP_NAME=Mailikan
APP_VERSION=1.0.0

# Logging
LOG_LEVEL=info
LOG_FILE=/var/log/mailikan/app.log

# Optional: Database Configuration (falls Sie später eine Datenbank hinzufügen)
# DATABASE_URL=postgresql://user:password@localhost:5432/mailikan

# Optional: Email Service Configuration
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password
